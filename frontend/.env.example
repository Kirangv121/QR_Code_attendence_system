# Backend origin (no trailing slash). The app appends /api if the URL does not already end with /api.
# Use either:
#   VITE_API_URL=http://localhost:5000
#   VITE_API_URL=http://localhost:5000/api
VITE_API_URL=http://localhost:5000
